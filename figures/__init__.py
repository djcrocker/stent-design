"""Figures: scripts in one place, rendered output split.

    figures/scripts/  Figure code.
    figures/dev/      Rendered exploratory output.
    figures/paper/    Rendered publication-ready output. (not soon)

Only `scripts/` is tracked; the two output dirs are gitignored down to a .gitkeep, because renders regenerate.
"""
